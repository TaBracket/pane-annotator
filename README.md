# Pane Annotator

A lightweight, browser-based image annotation tool.  
Draw and move simple shapes (ellipses) on top of images — then export the results as JSON.  
Pure **HTML + CSS + JavaScript**. No dependencies. Works offline.

---

## ✨ Features
- Draw ellipses over images
- Move shapes interactively
- Lightweight — pure vanilla JS
- Works offline (just open the file)
- Minimal, beginner-friendly codebase
- Export annotations as JSON (planned extensions)

---

## 📦 Project Structure
